import { PARTNERS } from '../shared/partners';
// dont have to capitalize,  state is the state from the handled data
export const Partners = (state = PARTNERS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};